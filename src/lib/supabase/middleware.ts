/**
 * @deprecated Use @/lib/middleware/edge-supabase — middleware must not import this file.
 * Kept for backwards compatibility during migration.
 */
export { refreshSupabaseSession as updateSupabaseSession } from "@/lib/middleware/edge-supabase";
